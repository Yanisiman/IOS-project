NAME:
	cp - copy files and directories
SYNOPSIS:
	cp SOURCE DEST
	cp SOURCE … DIRECTORY
	cp DIRECTORY DEST
 
DESCRIPTION:
   	Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY
SEE ALSO:
   	mv(1), ls(1), rm(1)
 
REPORTING BUGS:
   	Only working from file to file for now. Error when trying to open a directory with the open() system call. Need to fix this.
   	Write down here error that you have detected in your program and you can not correct. For example if you can not guarantee the answer when the argument is a number.
 
AUTHORS:
	Written by Yanis Chaabane   
