NAME:
	mv - move (rename) files
SYNOPSIS:
	mv SOURCE DEST
	mv SOURCE... DIRECTORY
 
DESCRIPTION:
   	move SOURCE(s) to DIRECTORY or Rename SOURCE to DEST(the last one is not implemented because in terminus you can rename with mv)

SEE ALSO:
   	cp(1), rm(1), mkdir(1), touch(1), rename(2)
 
REPORTING BUGS:
   	Write down here error that you have detected in your program and you can not correct. For example if you can not guarantee the answer when the argument is a number.
 
AUTHORS:
	Written by Jon
